{
  "name": "cuota-justa",
  "version": "1.0.0",
  "private": true,
  "description": "Panel de cuotas de Bet365 (la versión de claude.ai, lista para ejecutar en local).",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node servidor.mjs",
    "cargar": "node cargar-cuotas.mjs"
  }
}
